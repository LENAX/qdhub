# QDHub 快速入门指南

本指南将帮助你快速上手 QDHub，完成从创建数据源到爬取元数据的完整流程。

## 前置条件

1. QDHub 服务已启动（默认端口 8080）
2. 已准备好数据源的 Token（如 Tushare Token）
3. 可以使用 `curl` 或 Postman 等工具调用 API

## 第一步：验证服务状态

### 1.1 检查服务是否运行

```bash
curl http://localhost:8080/api/v1/workflows
```

如果返回工作流列表，说明服务正常运行。

### 1.2 确认内建工作流已初始化

```bash
curl http://localhost:8080/api/v1/workflows | jq '.data[] | select(.is_system == true)'
```

应该看到 4 个内建工作流：
- `builtin:metadata_crawl` - 元数据爬取
- `builtin:create_tables` - 创建数据表
- `builtin:batch_data_sync` - 批量数据同步
- `builtin:realtime_data_sync` - 实时数据同步

## 第二步：创建数据源

### 2.1 创建 Tushare 数据源

```bash
curl -X POST http://localhost:8080/api/v1/datasources \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tushare",
    "description": "Tushare Pro Data Source",
    "base_url": "http://api.tushare.pro",
    "doc_url": "https://tushare.pro/document/2"
  }'
```

**响应示例：**
```json
{
  "code": 201,
  "data": {
    "id": "ds_abc123",
    "name": "Tushare",
    "description": "Tushare Pro Data Source",
    "base_url": "http://api.tushare.pro",
    "doc_url": "https://tushare.pro/document/2",
    "created_at": "2024-01-17T10:00:00Z"
  }
}
```

**保存数据源 ID：**
```bash
export DATA_SOURCE_ID="ds_abc123"  # 替换为实际返回的ID
```

### 2.2 验证数据源创建

```bash
curl http://localhost:8080/api/v1/datasources/$DATA_SOURCE_ID
```

## 第三步：设置 Token

如果数据源需要 Token 认证，需要先设置：

```bash
curl -X POST http://localhost:8080/api/v1/datasources/$DATA_SOURCE_ID/token \
  -H "Content-Type: application/json" \
  -d '{
    "token": "your_tushare_token_here"
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "Token saved successfully"
}
```

## 第四步：执行元数据爬取

### 4.1 使用内建工作流爬取元数据

使用快捷方式（推荐）：

```bash
curl -X POST http://localhost:8080/api/v1/workflows/built-in/metadata_crawl/execute \
  -H "Content-Type: application/json" \
  -d "{
    \"trigger_type\": \"manual\",
    \"trigger_params\": {
      \"data_source_id\": \"$DATA_SOURCE_ID\",
      \"data_source_name\": \"tushare\",
      \"max_api_crawl\": 0
    }
  }"
```

**参数说明：**
- `data_source_id`: 数据源 ID（从步骤 2.1 获取）
- `data_source_name`: 数据源名称，必须与数据源适配器名称一致（如 "tushare"）
- `max_api_crawl`: 最大爬取 API 数量，0 表示不限制

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "instance_id": "inst_xyz789",
    "status": "started"
  }
}
```

**保存实例 ID：**
```bash
export INSTANCE_ID="inst_xyz789"  # 替换为实际返回的ID
```

### 4.2 使用工作流 ID 执行（替代方式）

如果知道工作流的固定 ID：

```bash
curl -X POST http://localhost:8080/api/v1/workflows/builtin:metadata_crawl/execute \
  -H "Content-Type: application/json" \
  -d "{
    \"trigger_type\": \"manual\",
    \"trigger_params\": {
      \"data_source_id\": \"$DATA_SOURCE_ID\",
      \"data_source_name\": \"tushare\",
      \"max_api_crawl\": 0
    }
  }"
```

## 第五步：监控工作流执行

### 5.1 查看工作流实例状态

```bash
curl http://localhost:8080/api/v1/instances/$INSTANCE_ID/progress
```

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "instance_id": "inst_xyz789",
    "status": "Running",
    "progress": 45.5,
    "task_count": 10,
    "completed_task": 4,
    "failed_task": 0,
    "started_at": "2024-01-17T10:05:00Z"
  }
}
```

**状态说明：**
- `Ready`: 已就绪，等待执行
- `Running`: 正在执行
- `Success`: 执行成功
- `Failed`: 执行失败
- `Paused`: 已暂停
- `Cancelled`: 已取消

### 5.2 查看任务实例列表

```bash
curl http://localhost:8080/api/v1/instances/$INSTANCE_ID/tasks
```

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {
      "id": "task_001",
      "name": "FetchCatalog",
      "status": "Success",
      "start_time": "2024-01-17T10:05:01Z",
      "end_time": "2024-01-17T10:05:03Z"
    },
    {
      "id": "task_002",
      "name": "ParseCatalog",
      "status": "Running",
      "start_time": "2024-01-17T10:05:03Z"
    }
  ]
}
```

### 5.3 轮询检查完成状态

```bash
# 创建检查脚本
cat > check_status.sh << 'EOF'
#!/bin/bash
INSTANCE_ID=$1
while true; do
  STATUS=$(curl -s http://localhost:8080/api/v1/instances/$INSTANCE_ID/progress | jq -r '.data.status')
  PROGRESS=$(curl -s http://localhost:8080/api/v1/instances/$INSTANCE_ID/progress | jq -r '.data.progress')
  echo "[$(date +%H:%M:%S)] Status: $STATUS, Progress: $PROGRESS%"
  
  if [ "$STATUS" = "Success" ] || [ "$STATUS" = "Failed" ]; then
    echo "Workflow completed with status: $STATUS"
    break
  fi
  sleep 5
done
EOF

chmod +x check_status.sh
./check_status.sh $INSTANCE_ID
```

## 第六步：查看爬取的元数据

### 6.1 查看数据源的所有 API

```bash
curl http://localhost:8080/api/v1/datasources/$DATA_SOURCE_ID/apis | jq '.data[] | {id, name, display_name, description}'
```

**响应示例：**
```json
[
  {
    "id": "api_001",
    "name": "stock_basic",
    "display_name": "股票基本信息",
    "description": "获取股票基本信息"
  },
  {
    "id": "api_002",
    "name": "daily",
    "display_name": "日线行情",
    "description": "获取日线行情数据"
  }
]
```

### 6.2 查看 API 详情

```bash
curl http://localhost:8080/api/v1/apis/{api_id} | jq '.data'
```

### 6.3 查看 API 分类

```bash
curl http://localhost:8080/api/v1/datasources/$DATA_SOURCE_ID/categories | jq '.data'
```

## 常见问题

### Q1: 工作流执行失败怎么办？

**检查步骤：**

1. 查看工作流实例状态：
```bash
curl http://localhost:8080/api/v1/instances/$INSTANCE_ID/progress | jq '.data.error_message'
```

2. 查看失败的任务：
```bash
curl http://localhost:8080/api/v1/instances/$INSTANCE_ID/tasks | jq '.data[] | select(.status == "Failed")'
```

3. 检查数据源配置：
   - 确认 `data_source_name` 是否正确（必须与适配器名称一致）
   - 确认 Token 是否已设置
   - 确认数据源 URL 是否正确

### Q2: 如何重新执行失败的工作流？

工作流执行失败后，可以：
1. 修复问题（如设置 Token、修正参数等）
2. 重新提交执行请求（使用相同的参数）

### Q3: 如何查看工作流执行日志？

查看服务器日志：
```bash
tail -f logs/qdhub.log
```

或查看 Task Engine 的日志输出（在服务器控制台）。

### Q4: 元数据爬取需要多长时间？

取决于：
- 数据源的 API 数量
- 网络速度
- 数据源服务器的响应速度

通常：
- 少量 API（< 10个）：1-2 分钟
- 中等数量（10-50个）：5-10 分钟
- 大量 API（> 50个）：10-30 分钟

### Q5: 如何限制爬取的 API 数量？

设置 `max_api_crawl` 参数：
```json
{
  "trigger_params": {
    "data_source_id": "...",
    "data_source_name": "tushare",
    "max_api_crawl": 10  // 只爬取前10个API
  }
}
```

## 下一步

完成元数据爬取后，你可以：

1. **创建数据表**：使用 `create_tables` 工作流根据元数据创建数据库表
2. **批量同步数据**：使用 `batch_data_sync` 工作流同步历史数据
3. **实时同步数据**：使用 `realtime_data_sync` 工作流设置定时同步

详细步骤请参考：
- [README.md](../README.md)
- [QDHub设计文档.md](./QDHub设计文档.md)

## 完整示例脚本

```bash
#!/bin/bash
# QDHub 元数据爬取完整示例

BASE_URL="http://localhost:8080"

echo "=== 步骤 1: 创建数据源 ==="
DATA_SOURCE_RESP=$(curl -s -X POST $BASE_URL/api/v1/datasources \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tushare",
    "description": "Tushare Pro Data Source",
    "base_url": "http://api.tushare.pro",
    "doc_url": "https://tushare.pro/document/2"
  }')

DATA_SOURCE_ID=$(echo $DATA_SOURCE_RESP | jq -r '.data.id')
echo "数据源 ID: $DATA_SOURCE_ID"

echo -e "\n=== 步骤 2: 设置 Token ==="
read -p "请输入 Tushare Token: " TOKEN
curl -X POST $BASE_URL/api/v1/datasources/$DATA_SOURCE_ID/token \
  -H "Content-Type: application/json" \
  -d "{\"token\": \"$TOKEN\"}"

echo -e "\n=== 步骤 3: 执行元数据爬取 ==="
INSTANCE_RESP=$(curl -s -X POST $BASE_URL/api/v1/workflows/built-in/metadata_crawl/execute \
  -H "Content-Type: application/json" \
  -d "{
    \"trigger_type\": \"manual\",
    \"trigger_params\": {
      \"data_source_id\": \"$DATA_SOURCE_ID\",
      \"data_source_name\": \"tushare\",
      \"max_api_crawl\": 0
    }
  }")

INSTANCE_ID=$(echo $INSTANCE_RESP | jq -r '.data.instance_id')
echo "工作流实例 ID: $INSTANCE_ID"

echo -e "\n=== 步骤 4: 监控执行状态 ==="
while true; do
  STATUS_RESP=$(curl -s $BASE_URL/api/v1/instances/$INSTANCE_ID/progress)
  STATUS=$(echo $STATUS_RESP | jq -r '.data.status')
  PROGRESS=$(echo $STATUS_RESP | jq -r '.data.progress')
  echo "[$(date +%H:%M:%S)] 状态: $STATUS, 进度: $PROGRESS%"
  
  if [ "$STATUS" = "Success" ] || [ "$STATUS" = "Failed" ]; then
    break
  fi
  sleep 5
done

echo -e "\n=== 步骤 5: 查看爬取的 API ==="
curl -s $BASE_URL/api/v1/datasources/$DATA_SOURCE_ID/apis | jq '.data[] | {name, display_name}'

echo -e "\n完成！"
```

保存为 `crawl_metadata.sh`，运行：
```bash
chmod +x crawl_metadata.sh
./crawl_metadata.sh
```
